// Initialize the whiteboard when the document is loaded
document.addEventListener('DOMContentLoaded', () => {
    // Create new whiteboard instance
    const whiteboard = new Whiteboard('whiteboard');
});
